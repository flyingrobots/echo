# SPDX-License-Identifier: Apache-2.0
# © James Ross Ω FLYING•ROBOTS <https://github.com/flyingrobots>
[package]
name = "warp-wasm"
version = "0.1.0"
edition = "2021"
rust-version = "1.90.0"
description = "Echo WASM: wasm-bindgen bindings for tools and web"
license = "Apache-2.0"
repository = "https://github.com/flyingrobots/echo"
readme = "README.md"
keywords = ["echo", "wasm", "ecs", "deterministic"]
categories = ["wasm", "game-engines"]

[lib]
# cdylib for WASM; rlib allows native `cargo test` and downstream integration tests.
crate-type = ["cdylib", "rlib"]

[features]
default = []
console-panic = ["console_error_panic_hook", "web-sys"]
# Enable the warp-core engine kernel implementation.
# Apps compile with this feature to get a real Engine behind the WASM exports.
engine = ["dep:warp-core"]

[dependencies]
echo-registry-api = { workspace = true }
echo-wasm-abi = { workspace = true }
warp-core = { workspace = true, optional = true }
wasm-bindgen = "0.2.106"
js-sys = "0.3.83"
web-sys = { version = "0.3.83", optional = true, features = ["console"] }
console_error_panic_hook = { version = "0.1.7", optional = true }
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
warp-core = { workspace = true }
serde-value = "0.7"

[package.metadata.wasm-pack.profile.release]
wasm-opt = false


[lints]
workspace = true
